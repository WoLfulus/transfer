package service

// LabelVersion contains the service version label used to check the service version
const LabelVersion = "com.wolfulus.transfer.version"

// LabelManaged tells if the service is being managed by the cli
const LabelManaged = "com.wolfulus.transfer.managed"
